package eu.trentorise.game.model


rule "update green points"
no-loop
when
    InputData( $walk : data["walk"])
    $pc : PointConcept(name == "green leaves")
then
	log("apply \'SETSCORE update green points\'");
    $pc.setScore($pc.getScore() + (int) $walk);
    update($pc);
end